# For Physical Device on same WiFi (Expo Go)
API_BASE_URL=http://172.18.43.165:8000

# For iOS Simulator / Web (uncomment if testing on iOS Simulator or web)
# API_BASE_URL=http://localhost:8000

# For Android Emulator (uncomment if testing on Android Emulator)
# API_BASE_URL=http://10.0.2.2:8000

# Microservices (using same base URL since it's monolith for now)
USER_SERVICE_URL=http://172.18.43.165:8000
TICKLIST_SERVICE_URL=http://172.18.43.165:8000
CHATBOT_SERVICE_URL=http://172.18.43.165:8000
LIBRARY_SERVICE_URL=http://172.18.43.165:8000









# Example environment variables for Supabase
# Copy this file to `.env` and fill the values before running the app.
# Keep the real `.env` file out of source control (add to .gitignore).

# Supabase project URL (found in Supabase project settings)
SUPABASE_URL=

# Supabase anon/public API key (for client access)
SUPABASE_ANON_KEY=
# Supabase service role key (server only - keep secret)
# Only required if you run server-side functions that need elevated privileges.
SUPABASE_SERVICE_ROLE_KEY=
# Optional: custom Supabase storage bucket name
SUPABASE_STORAGE_BUCKET=public

# Optional: additional runtime flags
NODE_ENV=development

# Example: enable debug logging in supabase-js (true/false)
SUPABASE_DEBUG=false

# If you are using Realtime or Edge functions and need a custom websocket endpoint
# SUPABASE_REALTIME_URL=wss://your-realtime-endpoint.supabase.co

# NOTES:
# - Never commit your real keys. Use CI/CD secrets for production deployments.
# - For React Native / Expo, load env vars using a library like react-native-dotenv
#   or configure them through app config (app.json/app.config.js) and EAS secrets.